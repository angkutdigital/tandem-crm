{
  "name": "tandem-crm",
  "version": "0.1.0",
  "description": "The engine half of Tandem, an embeddable, event-sourced CRM for Postgres: leads, agents, sales activity, commissions, and disputes. No servers, no vendor lock-in. The installable admin (tandem-camp) is a separate package -- see packages/camp.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./db": {
      "types": "./dist/db/index.d.ts",
      "import": "./dist/db/index.js"
    }
  },
  "sideEffects": false,
  "files": [
    "dist",
    "db/migrations",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/angkutdigital/tandem-crm"
  },
  "keywords": [
    "postgres",
    "commission",
    "affiliate",
    "partner",
    "event-sourcing",
    "embeddable"
  ],
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
